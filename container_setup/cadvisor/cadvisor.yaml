prometheus:
  enabled: true
  listenAddress: ":8081"

docker:
  endpoint: "unix:///var/run/docker.sock"
  timeout: "5s"
  version: "1.24"

kubelet:
  enabled: true
  url: "http://localhost:10255"
  timeout: "5s"

healthzPort: 8085
